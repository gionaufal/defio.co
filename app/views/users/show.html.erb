<% if @user %>
  <h5><%= @user.username %></h5>
  <% if @user != current_user %>
    <%= link_to 'follow' %>
  <% end %>
  <div class="row">
    <section class='posts'>
      <%= render @posts %>
    </section>
  </div>
<% end %>
